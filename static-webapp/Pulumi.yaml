name: ${PROJECT}
description: ${DESCRIPTION}
runtime: nodejs

template:
  description: A TypeScript program to deploy a static website using Azure Static Web Apps
  config:
    azure-native:location:
      description: The Azure region to deploy into
      default: westeurope
    path:
      description: The path to the folder containing the website
      default: ./www
